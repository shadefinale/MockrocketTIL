<div class="row">
  <div class="col-md-10 col-md-offset-1 pull-left">
    <div class="row">
    <%= prev_button(@post) %>
    <%= next_button(@post) %>
    <%= new_post_button(current_user, @post.author.id) %>
  </div>
  </div>
</div>
<%= render partial: 'post', locals: { post: @post } %>
